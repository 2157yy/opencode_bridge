# Deep Interview Transcript — TEST_ISSUES

## Summary
User clarified that `TEST_ISSUES.md` is not just documentation to rewrite. All listed problems must be solved in this pass.

## Clarifications obtained
- Issue 6/7 (visible terminal/session per worker; each agent is a full CLI with skills/tools/MCP/hooks) are in scope.
- macOS Terminal.app and Linux GUI terminal adapters are also in scope, not tmux only.
- Per-worker LLM configuration is in scope and must be persisted.
- `apiKey` storage strategy: store in registry plaintext, but mask it in CLI output.
- The work is still brownfield and should preserve the existing bridge lifecycle unless a minimal change is required.

## Pressure-pass recap
- Initial guess that only the test docs needed editing was rejected.
- Initial guess that tmux-only terminal handling was sufficient was rejected.
- Initial guess that masked-only persistence for `apiKey` was sufficient was refined to plaintext registry storage plus masked output.

## Result
The task is now ready for planning/implementation against the clarified requirements in `.omx/specs/deep-interview-test-issues.md`.
